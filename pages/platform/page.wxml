<view>
    <canvas canvas-id="canvas" ></canvas>
    <MergeLayer container="{{container}}" items="{{items}}"/>
    <MoveContainer
    container="{{container}}"
    />
    <UploadImg bindchange="uploadImg">
        上传
    </UploadImg>
    <GetImageData img="{{img}}" bindchange="getImageData"/>
</view>